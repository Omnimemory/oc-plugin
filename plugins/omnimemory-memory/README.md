# OmniMemory Memory Slot

OpenClaw memory-slot plugin backed by OmniMemory router v2.

This plugin registers `memory_search` only. `memory_get` is intentionally absent because the current v2 backend does not expose a graph/detail read path.
